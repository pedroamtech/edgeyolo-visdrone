type: "yolo"

dataset_path: "C:/Users/pedroam/Documents/Data-Augmentation/Datasets/VisDrone-aug"  # <-- ajusta esta ruta cuando tengas el dataset aumentado

kwargs:
  suffix: "jpg"
  use_cache: true

train:
  image_dir: "train/images"
  label: "train/labels"

val:
  image_dir: "validation/images"
  label: "validation/labels"

test:
  test_dir: "validation/images"

segmentaion_enabled: false

names: ['persona', 'ignorar']
